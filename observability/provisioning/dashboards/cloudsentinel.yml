apiVersion: 1

providers:
  - name: CloudSentinel
    orgId: 1
    folder: CloudSentinel
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
