# CloudSentinel

CloudSentinel est un framework DevSecOps cloud qui combine shift-left, shift-right, OPA, exceptions DefectDojo, drift detection et remediation runtime.

Documentation principale: [docs/README.md](docs/README.md)

Commandes rapides:

```bash
cp .env.template .env
make opa-test
make test-python
make scan
```
